# Sanity Configuration
NEXT_PUBLIC_SANITY_PROJECT_ID=aibflqfk
NEXT_PUBLIC_SANITY_DATASET=production

# Only needed to run the maintenance scripts in scripts/ (migration, backfill).
# The site itself is read-only against Sanity and does not need a write token.
SANITY_API_WRITE_TOKEN=your_sanity_write_token_here

# PayPal.Me username (with or without @) — the site redirects here for payment.
NEXT_PUBLIC_PAYPAL_ME_USERNAME=palirafflefundraiser
